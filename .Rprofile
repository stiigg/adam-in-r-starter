options(warn = 1)
if (requireNamespace("targets", quietly = TRUE)) {
  targets::tar_option_set(packages = c(
    "dplyr","tidyr","lubridate","readr","purrr","stringr","tibble","yaml","validate","xportr"
  ))
}
